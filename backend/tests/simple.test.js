describe('Simple Test Suite', () => {
  it('should pass a simple true is true test', () => {
    expect(true).toBe(true);
  });

  it('should correctly sum two numbers', () => {
    expect(1 + 2).toBe(3);
  });
});
